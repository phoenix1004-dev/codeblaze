export type MenuLink = {
  title: string;
  link: string;
};

export type HEADER_BUTTON = {
  title: string;
  link: string;
  icon: any;
};

export type CursorPosition = {
  left: number;
  width: number;
  opacity: number;
};
